# Case Functions
Catalyst serverless functions for CaseMaster CRUD and CrimeNo/CaseNo generation.
Endpoints: POST /cases, GET /cases/{id}, GET /cases (filtered search),
PATCH /cases/{id}/status, GET /cases/{id}/timeline
Tables touched: CaseMaster, CaseCategory, CaseStatusMaster, CrimeHead, CrimeSubHead
