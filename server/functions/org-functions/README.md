# Org / Geography Functions
Catalyst serverless functions for reference data (read-mostly).
Endpoints: GET /units/{unitId}, GET /units/{unitId}/children,
GET /employees/{employeeId}
Tables touched: Unit, UnitType, District, State, Employee, Rank, Designation
